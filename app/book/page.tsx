import BookingClient from "./BookingClient";

export const metadata = {
  title: "Book a session — Marigold & Frame Photography",
};

export default function BookPage() {
  return <BookingClient />;
}
